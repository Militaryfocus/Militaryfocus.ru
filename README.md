# 🚀 МойБлог - Современное ядро блога на Python Flask

Полнофункциональное веб-приложение блога, созданное с использованием Flask и современных веб-технологий.

## ✨ Особенности

### 🤖 **ИИ Система автоматического наполнения контентом**
- **Генерация постов неотличимых от человеческих**
- **Автоматические реалистичные комментарии**
- **Планировщик публикаций**
- **Контроль качества контента**
- **Разнообразие тем и стилей**
- **Интеграция с OpenAI API**

### 🔐 Аутентификация и авторизация
- Регистрация и вход пользователей
- Система ролей (администратор/пользователь)
- Защищенные маршруты
- Управление профилем

### 📝 Управление контентом
- Создание, редактирование и удаление постов
- Поддержка Markdown разметки
- Система категорий и тегов
- Изображения для постов
- Черновики и публикация

### 💬 Система комментариев
- Комментарии к постам
- Ответы на комментарии (вложенность)
- Модерация комментариев
- Уведомления

### 🎨 Современный интерфейс
- Адаптивный дизайн (Bootstrap 5)
- Темная/светлая тема
- Анимации и переходы
- Интерактивные элементы

### 🔍 Поиск и навигация
- Полнотекстовый поиск
- Фильтрация по категориям
- Пагинация
- Популярные и рекомендуемые посты

### 🛠 Админ-панель
- Панель управления
- Статистика сайта
- Управление пользователями
- Модерация комментариев
- Управление категориями

## 🚀 Быстрый старт

### Требования
- Python 3.8+
- pip

### Установка

1. **Клонируйте репозиторий:**
```bash
git clone <repository-url>
cd blog
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\\Scripts\\activate  # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте переменные окружения:**
Скопируйте `.env` файл и настройте под свои нужды:
```bash
cp .env .env.local
```

Основные переменные:
- `SECRET_KEY` - секретный ключ для Flask
- `DATABASE_URL` - URL базы данных
- `FLASK_DEBUG` - режим отладки

5. **Запустите приложение:**
```bash
python app.py
```

Приложение будет доступно по адресу: http://localhost:5000

### Первый запуск

При первом запуске автоматически создается администратор:
- **Логин:** admin
- **Пароль:** admin123

⚠️ **Обязательно смените пароль после первого входа!**

## 📁 Структура проекта

```
blog/
├── app.py                 # Точка входа приложения
├── requirements.txt       # Зависимости Python
├── .env                  # Переменные окружения
├── blog/                 # Основной пакет приложения
│   ├── __init__.py       # Фабрика приложений
│   ├── models.py         # Модели базы данных
│   ├── forms.py          # WTForms формы
│   ├── routes/           # Маршруты приложения
│   │   ├── main.py       # Основные маршруты
│   │   ├── auth.py       # Аутентификация
│   │   ├── blog.py       # Блог функционал
│   │   └── admin.py      # Админ-панель
│   ├── templates/        # HTML шаблоны
│   │   ├── base.html     # Базовый шаблон
│   │   ├── index.html    # Главная страница
│   │   ├── auth/         # Шаблоны аутентификации
│   │   ├── blog/         # Шаблоны блога
│   │   └── admin/        # Шаблоны админки
│   └── static/           # Статические файлы
│       ├── css/          # Стили
│       ├── js/           # JavaScript
│       └── uploads/      # Загруженные файлы
└── migrations/           # Миграции базы данных
```

## 🗄 Модели данных

### User (Пользователь)
- Базовая информация (имя, email, пароль)
- Роли и права доступа
- Профиль и аватар

### Post (Пост)
- Заголовок, содержимое, краткое описание
- Категория и теги
- Статус публикации
- Метаданные (просмотры, дата создания)

### Category (Категория)
- Название и описание
- Цветовая схема
- URL slug

### Comment (Комментарий)
- Содержимое комментария
- Связь с постом и автором
- Вложенные комментарии
- Модерация

### Tag (Тег)
- Название и slug
- Связь многие-ко-многим с постами

## 🎨 Технологии

### Backend
- **Flask** - веб-фреймворк
- **SQLAlchemy** - ORM для работы с БД
- **Flask-Login** - управление сессиями
- **Flask-WTF** - обработка форм
- **Flask-Migrate** - миграции БД
- **Werkzeug** - утилиты и безопасность

### Frontend
- **Bootstrap 5** - CSS фреймворк
- **Font Awesome** - иконки
- **JavaScript ES6+** - интерактивность
- **EasyMDE** - Markdown редактор

### Дополнительно
- **Markdown** - разметка контента
- **Bleach** - очистка HTML
- **Pillow** - обработка изображений
- **Python-slugify** - генерация URL slug

## 🔧 Конфигурация

### Переменные окружения (.env)

```env
# Основные настройки
SECRET_KEY=your-secret-key-here
FLASK_DEBUG=True
FLASK_HOST=0.0.0.0
FLASK_PORT=5000

# База данных
DATABASE_URL=sqlite:///blog.db

# Загрузка файлов
UPLOAD_FOLDER=static/uploads
MAX_CONTENT_LENGTH=16777216

# Пагинация
POSTS_PER_PAGE=5
COMMENTS_PER_PAGE=10

# Email (опционально)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
```

### Настройка базы данных

По умолчанию используется SQLite. Для production рекомендуется PostgreSQL:

```env
DATABASE_URL=postgresql://username:password@localhost/blog_db
```

## 🚀 Развертывание

### Локальная разработка
```bash
export FLASK_ENV=development
export FLASK_DEBUG=True
python app.py
```

### Production

1. **Настройте переменные окружения:**
```env
FLASK_ENV=production
FLASK_DEBUG=False
SECRET_KEY=your-strong-secret-key
DATABASE_URL=postgresql://...
```

2. **Используйте WSGI сервер:**
```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:8000 app:app
```

3. **Настройте веб-сервер (nginx):**
```nginx
server {
    listen 80;
    server_name yourdomain.com;
    
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
    
    location /static {
        alias /path/to/blog/static;
    }
}
```

## 🤝 Участие в разработке

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Добавьте тесты
5. Отправьте Pull Request

## 📝 Лицензия

MIT License - см. файл LICENSE

## 🆘 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте документацию
2. Поищите в Issues
3. Создайте новый Issue с подробным описанием

## 🔄 Обновления

Следите за обновлениями в разделе Releases. Рекомендуется регулярно обновлять зависимости:

```bash
pip install --upgrade -r requirements.txt
```

## 🤖 ИИ Система автоматического наполнения

### 🎯 Особенности ИИ системы:

- **Неотличимый от человеческого контент** - посты выглядят как написанные реальными людьми
- **Разнообразие тем** - технологии, наука, общество, бизнес, образование
- **Естественные стили** - от научно-популярного до разговорного
- **Автоматические комментарии** - реалистичные диалоги под постами
- **Планировщик публикаций** - автоматическая публикация по расписанию
- **Контроль качества** - оценка читаемости и уникальности
- **Markdown форматирование** - красивое оформление текста

### 🚀 Команды управления ИИ:

```bash
# Демо-наполнение блога
python demo_populate.py

# Управление через командную строку
python ai_manager.py stats              # Статистика
python ai_manager.py generate 10        # Создать 10 постов
python ai_manager.py test              # Тест генератора
python ai_manager.py scheduler         # Запустить планировщик
python ai_manager.py setup --with-content  # Первоначальная настройка
```

### 🎛️ ИИ Панель управления:

Доступна по адресу: `http://localhost:5000/ai/ai-dashboard`

- Генерация одиночных и массовых постов
- Настройки качества и стиля
- Аналитика ИИ контента
- Управление ИИ пользователями
- Экспорт и очистка данных

### ⚙️ Настройки ИИ (.env):

```env
AI_CONTENT_ENABLED=True        # Включить ИИ систему
AI_AUTO_PUBLISH=True           # Автопубликация
AI_GENERATE_COMMENTS=True      # Генерация комментариев
AI_POSTS_PER_DAY=3            # Постов в день
AI_MIN_QUALITY=0.7            # Минимальное качество
OPENAI_API_KEY=your-key       # OpenAI API (опционально)
```

### 🎭 Как это работает:

1. **Генератор тем** - выбирает актуальные темы из разных областей
2. **Создание заголовков** - генерирует привлекательные заголовки
3. **Написание контента** - создает структурированные статьи с введением, основной частью и заключением
4. **Добавление человечности** - вставляет личные мнения, эмоции, разговорные элементы
5. **Форматирование** - применяет Markdown разметку для красивого отображения
6. **Генерация метаданных** - создает теги, описания, категории
7. **Создание комментариев** - добавляет реалистичные комментарии от ИИ пользователей

### 🎨 Примеры сгенерированного контента:

**Заголовки:**
- "Как искусственный интеллект изменит наше будущее"
- "10 фактов о квантовых вычислениях, которые вас удивят"
- "Революция в блокчейне: что нас ждет в 2024 году"

**Стили:**
- Научно-популярный с фактами и исследованиями
- Разговорный с личными мнениями
- Аналитический с глубоким разбором
- Практический с примерами применения

---

**🤖 Создано с помощью ИИ и ❤️ на Python Flask**